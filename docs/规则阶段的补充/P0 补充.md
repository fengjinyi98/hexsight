结论：**建议先继续完善 P0 收口，暂缓进入 P1。**  (首次)
现在 P0 的“评分器骨架”已经成型并通过测试，但“工程可持续性底座”还没真正落地；直接进 P1 会把权重、阈值、版本差异继续写进代码里，后续版本更新成本会变高。

| P0 项 | 当前状态 | 判断 |
|---|---|---|
| `LineupProfileBuilder` | 已实现，三模式有测试 | 可收口 |
| `OpeningRouteClassifier` | 已实现基础规则 | 可收口前小修 |
| `TransitionStrengthScorer` | 已实现基础评分 | 可收口前小修 |
| `LineupFitScorer` | 已实现 Top 阵容评分 | 需接入规则包 |
| `ItemFitScorer` | 已实现基础装备方向 | 需补散件逻辑 |
| `RerollEligibilityScorer` | 已实现基础赌狗资格 | 可收口前小修 |
| `DecisionPlanner` | 已实现 RuleOutput 组装 | 可作为 P0+ |
| `RulePackLoader` | 文档有，代码缺失 | 需要补 |
| `SchemaValidator` | 文档有，代码缺失 | 需要补 |
| `PatchOverrideLoader` | 文档有，代码缺失 | 可做空实现或 P1 前置 |

验证结果：

| 命令 | 结果 |
|---|---|
| `cargo test --workspace` | 通过：Rust engine 48 tests + FFI 5 tests |
| `cd swift && swift test && swift build` | 通过：Swift 17 tests |
| `make build` | 通过 |
| `cargo check --workspace` | 通过，但有 13 个 warning |

主要问题：

| 优先级 | 问题 | 证据 |
|---|---|---|
| P0 必修 | `RulePack` 只有默认类型，缺少从 `config/rules/<version>/rule_pack.json` 加载 | `/Users/fengjinyi/Desktop/hexsight/crates/hexsight-core/src/rule_types.rs` 只有 `Default` |
| P0 必修 | `LineupFitScorer` 权重硬编码 | `/Users/fengjinyi/Desktop/hexsight/crates/hexsight-engine/src/lineup_fit_scorer.rs:92` |
| P0 必修 | `SchemaValidator` 缺失，版本更新无法自动发现字段缺失/ID 错位 | `rg` 未找到实现 |
| P0 建议 | `ItemFitScorer` 接收 `current_components` 但未使用 | `/Users/fengjinyi/Desktop/hexsight/crates/hexsight-engine/src/item_fit_scorer.rs:17` |
| P0 建议 | `OpeningRouteClassifier` 多个输入未使用，说明当前判断还偏静态 | `/Users/fengjinyi/Desktop/hexsight/crates/hexsight-engine/src/opening_route_classifier.rs:15` |
| P0 建议 | Rust 新增 13 个 warning | `cargo check --workspace` |

建议让 Claude 先补完这个 P0 收口清单：

| 顺序 | 任务 | 验收 |
|---|---|---|
| 1 | 新增 `config/rules/S18.1/rule_pack.json` | 能反序列化成 `RulePack` |
| 2 | 实现 `RulePackLoader` | 支持指定版本加载，失败回退默认规则包并给错误信息 |
| 3 | `LineupFitScorer` / `TransitionStrengthScorer` 接收 `RulePack` 权重 | 评分权重不再硬编码 |
| 4 | 实现 `SchemaValidator` | 输出字段缺失、ID 缺失、阵容解析率报告 |
| 5 | `ItemFitScorer` 使用散件输入 | 能判断可合成核心装和通用装 |
| 6 | 清理 Rust warning | `cargo check --workspace` warning 数降到 0 |
| 7 | 补回归测试 | 覆盖规则包加载、Schema 校验、权重影响评分 |

收口标准：

| 命令 | 要求 |
|---|---|
| `cargo check --workspace` | 0 warning |
| `cargo test --workspace` | 通过 |
| `cd swift && swift test && swift build` | 通过 |
| `make build` | 通过 |

我的判断：**P0 已经完成 70%-75%，但还没达到“可持续工程底座”标准。先补 P0，补完后再进 P1。**
