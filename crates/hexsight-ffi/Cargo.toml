[package]
name = "hexsight-ffi"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]
hexsight-core = { path = "../hexsight-core" }
hexsight-vision = { path = "../hexsight-vision" }
hexsight-memory = { path = "../hexsight-memory" }
hexsight-engine = { path = "../hexsight-engine" }
hexsight-llm = { path = "../hexsight-llm" }
serde_json = "1"
