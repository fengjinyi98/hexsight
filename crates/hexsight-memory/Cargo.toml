[package]
name = "hexsight-memory"
version.workspace = true
edition.workspace = true

[dependencies]
hexsight-core = { path = "../hexsight-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
