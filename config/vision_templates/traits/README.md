# 羁绊模板目录

## 用途

羁绊图标模板由 `hexsight-vision` 加载，用于左侧羁绊图标匹配，并映射到模板文件名对应的 trait id 或名称。

## 约定

- 文件名即识别结果，例如 `裁决使.txt`
- 支持 `png`、`jpg`、`jpeg`、`webp` 和文本位图 `txt`
- race/job 图标可直接放入本目录
- 文本位图中 `R/G/B/Y/P/#` 表示前景颜色，`.` 表示背景
