# 装备模板目录

## 用途

装备图标模板由 `hexsight-vision` 加载，用于装备席和棋子身上装备的图标匹配。

## 约定

- 文件名即识别名称，例如 `暴风大剑.txt`
- 支持 `png`、`jpg`、`jpeg`、`webp` 和文本位图 `txt`
- 文本位图中 `R/G/B/Y/P/#` 表示前景颜色，`.` 表示背景
- 下载脚本后续可落在 `scripts/`，并按散件、成装、转职排除重复或过期图标
